# Production environment — frontend deployed on Vercel
# Leave VITE_API_BASE empty so API calls go to the same origin (/api/...)
# This works because frontend and backend are deployed together on Vercel
VITE_API_BASE=
